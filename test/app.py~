from flask import Flask
app = Flask(__name__)

@app.route("/")
def button():
    return render_template(
        'button.html'
        )

@app.route("/auth"):
def authenticate ():
    print(app)
    print(request)
    print(request.args)
    return "Waaaa hooo HAAAH"

if __name__== "__main__":
    app.debug = True
    app.run()
